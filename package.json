{
  "name": "query",
  "version": "0.0.1",
  "description": "A functional approach to DOM manipulation.",
  "main": "dist/query.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "knrz",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.8.10",
    "gulp-changed": "^1.1.0",
    "gulp-concat": "^2.4.2",
    "gulp-uglify": "^1.0.2"
  }
}
